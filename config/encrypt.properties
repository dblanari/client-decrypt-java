# Encryption settings for payload encryption using Method 2 (manual CSR upload).
# Set this to the public encryption certificate you downloaded after uploading your CSR.
# For the demo below, a self-signed cert was generated at config/keys/demo-encryption.crt.
encryptionCertificatePath=config/keys/demo-encryption.crt

# Optional overrides (relative paths resolve from the project root)
# inputJson=examples/chekcoutDecryptedCardResponse.json
# outputJson=examples/encryptedPayload.json
